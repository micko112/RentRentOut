# ============================================================
# PRODUCTION PROFILE — sve osetljive vrednosti dolaze iz env
# Pokretanje: java -jar app.jar --spring.profiles.active=prod
# ============================================================

# --- Database ---
spring.datasource.url=jdbc:mysql://${DB_HOST}:${DB_PORT:3306}/${DB_NAME:rent_rent_out}?allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

# --- Liquibase ---
spring.liquibase.enabled=true
spring.liquibase.change-log=classpath:db/changelog/db.changelog-master.xml
spring.liquibase.drop-first=false

# --- Multipart upload ---
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=50MB

# --- JWT ---
jwt.secret=${JWT_SECRET}

# --- OAuth (public, non-sensitive) ---
google.client-id=1030670787389-cftefckkjmpgiv41okb87oatffou4e5k.apps.googleusercontent.com
facebook.app-id=1472036881067070
apple.client-id=com.rentrentout.web

# --- VAPID public key ---
app.vapid.public-key=BNkUoK6_XlgpiZzXNkuIx81VjfpHwvWeLHE07avjUnl4cX6Q01NxM4Ca5CWkNSNuoopzXnfZODMmLpiunPmhvyw
app.vapid.subject=mailto:izdajemiznajmljujem.rs@gmail.com

# --- Mail (pravi SMTP, ne Mailtrap) ---
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}

# --- Frontend URL ---
app.frontend.url=${FRONTEND_URL}/verify-email
app.frontend.base-url=${FRONTEND_URL}
app.reset-password.url=${FRONTEND_URL}/reset-password

# --- VAPID (Web Push) ---
app.vapid.private-key=${VAPID_PRIVATE_KEY}

# --- Facebook OAuth ---
facebook.app-secret=${FACEBOOK_APP_SECRET}

# --- Cloudinary ---
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME}
cloudinary.api-key=${CLOUDINARY_API_KEY}
cloudinary.api-secret=${CLOUDINARY_API_SECRET}

app.cookie.secure=true
encryption.phone-key=${ENCRYPTION_PHONE_KEY}

# --- Disable Swagger UI in production ---
springdoc.swagger-ui.enabled=false
springdoc.api-docs.enabled=false

sentry.dsn=${SENTRY_DSN}
sentry.traces-sample-rate=${SENTRY_TRACES_SAMPLE_RATE}
sentry.environment=${SENTRY_ENVIRONMENT}

sentry.exception-resolver-order=-2147483647